# Login 페이지 검증 프롬프트 (Claude Code용 — VS Code 터미널에 붙여넣기)

---

## 프롬프트 (아래 전체를 복사하세요)

src/app/public/login/ 페이지를 검증합니다.

### 핵심 대상 파일
- src/app/public/login/page.tsx
- src/app/public/login/login.module.css
- src/components/LoginForm.tsx

### 연관 파일 (함께 점검)
- API: src/app/api/public/auth/ 폴더 전체 (check-login-method, state, exchange, 각 OAuth callback)
- Auth 유틸: src/auth/ 폴더 전체 (cognito.ts, tokenManager.ts, google.ts, kakao.ts, naver.ts, apple.ts, user.ts, constants.ts)
- 미들웨어: src/middleware.ts

### 검증 방법
'페이지별_코드검증_질문리스트.md'를 읽고, 아래 순서대로 한 카테고리씩 검증하세요. 각 카테고리 완료 후 "다음 단계로 넘어갈까요?"라고 확인한 뒤 다음으로 진행하세요.

1단계: 구조 & 코드 품질
2단계: 보안 (특히 중요 — OAuth 흐름, 토큰 저장/갱신, CSRF, XSS, redirect URI 검증)
3단계: API 설계 (각 OAuth callback 포함)
4단계: 에러 처리 & UX (로그인 실패, 네트워크 에러, 토큰 만료 시 사용자 피드백)
5단계: 성능
6단계: 반응형 & 접근성
7단계: 앱 연동 대비 (모바일 앱에서 동일 인증 흐름 사용 가능한 구조인지)

문제 발견 시 심각도(높음/중간/낮음)를 표시하고 수정 코드를 함께 제시하세요.
